# 避難所検索機能を移植するための一括指示プロンプト

既存システムに、青森市の避難所検索機能を移植してください。以下の要件をすべて満たし、実装・データ統合・動作確認まで一度に完了してください。

## 前提

- Flaskなど、移植先システムの既存フレームワークと設計を優先する。
- 既存のログイン、トップページ、管理画面、指示ボードなど、避難所検索以外の機能は変更しない。
- 既存の避難所登録機能がある場合、その公開APIや登録データを壊さない。
- 地図にはLeafletとOpenStreetMapを使用してよい。既存の地図ライブラリがある場合はそれを優先する。
- 日本語UIを維持する。

## データソース

添付の `all_evacuation_sites_combined.csv` を避難所一覧の正規データとして使用する。CSVの列は次のとおり。

- `地域区分`
- `No`
- `地区(大字・町名)`
- `施設名称`
- `所在地`
- `洪水`
- `土砂災害`
- `高潮`
- `地震`
- `津波`
- `大規模な火事`
- `内水氾濫・火山現象`
- `指定避難所`

既存の別CSVを自動的に追加マージして、CSVにない施設や地域区分が混ざらないようにする。避難所の住所はCSVの `所在地` を正規値として表示する。

## 地域区分

CSVの地域コードを次の表示名に変換する。

| CSVの地域区分 | 画面表示 |
|---|---|
| `hokubu` | 北部 |
| `chuubu` | 中央(1) |
| `chuubu2` | 中央(2) |
| `toubu` | 東部 |
| `nanbu` | 南部 |
| `namioka` | 浪岡地区 |

CSVに存在しない区分を推測して施設へ割り当てない。地域区分のないデータは「未分類」として隔離するか、表示対象から除外して原因を明示する。

## 検索画面

避難所検索画面に以下を実装する。

1. 地図を表示する。
2. 赤い円形マーカーで、座標のある避難所を表示する。
3. 地域選択セレクトを表示する。
4. 地域を選択すると、その地域に属する `地区(大字・町名)` だけを地区選択セレクトへ表示する。
5. 地区選択で、同じ地域内の特定地区まで絞り込める。
6. 「全域」を選択できる。
7. 地域や地区を変更しても、全避難所の赤いマーカーを消さない。フィルターはマーカーの削除ではなく、対象範囲への地図移動と選択状態の更新に使う。
8. 地域を選択した場合は、その地域のマーカーが見える範囲へ移動する。
9. 地区を選択した場合は、その地区のマーカーが見える範囲へ移動する。
10. 選択地域に座標付き施設がない場合も画面を壊さず、地図中心を青森市へ戻してメッセージを表示する。

## 座標の安全性

施設名だけで座標を割り当てない。施設名が同名の別施設と混同される場合があるため、住所と地域区分を検証する。

- 青森市の許容範囲外の緯度経度は採用しない。
- 地域区分の大まかな範囲と矛盾する座標は採用しない。
- 他県、北海道、海上などの座標を地図に表示しない。
- 座標が不明または検証失敗した施設は、一覧/APIには残してもよいが、地図マーカーは表示しない。
- 座標ファイルに古い誤データがある場合、それを無条件に優先しない。
- `lat=0`、`lng=0`、文字列、null、不正な値をマーカーに渡さない。

## マーカー選択時の操作

避難所マーカーをクリックしたポップアップに、次を表示する。

- 施設名称
- 地域区分
- 地区(大字・町名)
- CSVの住所
- 収容人数など既存データにある詳細
- 「詳細」ボタン
- 「ルート案内」ボタン

「詳細」ボタンを押したら、選択した避難所の詳細パネルを地図の下または既存の詳細領域に表示する。

「ルート案内」ボタンは、緯度経度を目的地としてGoogle Mapsなどの経路検索を新しいタブで開く。URLに施設名や未検証の外部入力をそのままHTMLとして挿入せず、必ずHTMLエスケープする。

## APIと画面

既存の避難所APIがある場合は維持し、次の検索に対応させる。

- `/shelters`
- `/shelters?district=地域表示名`
- `/shelters?district=地域表示名&area=地区名`

APIの各施設には少なくとも次の値を含める。

- `id`
- `name`
- `district`
- `region_code`
- `area`
- `address`
- `lat`
- `lng`
- 既存の災害種別・説明項目

検索結果画面では、施設名だけでなく住所、地域、地区を表示する。既存の避難所検索画面と検索結果画面のルート、ナビゲーション、既存機能は維持する。

## 実装手順

1. 既存の避難所データ読み込み処理を確認する。
2. `all_evacuation_sites_combined.csv` を読み込めるようにする。
3. CSVの住所・地域・地区を正規データとして施設オブジェクトへ格納する。
4. 座標を検証し、異常座標を除外する。
5. APIに地域・地区を含める。
6. 検索画面に地域選択と地区選択を追加する。
7. フィルター変更時に全マーカーが消えないようにする。
8. 詳細ボタンとルート案内ボタンを追加する。
9. 検索結果画面にも住所・地域・地区を表示する。
10. 関係のない機能やファイルは変更しない。

## 必須の確認

実装後、次を実行して確認する。

- Pythonの構文チェック
- 主要な既存テスト
- `/shelter_search` が200を返すこと
- `/shelters` が正常なJSONを返すこと
- CSVの施設件数とAPIの施設件数が一致すること
- すべての施設に地域と地区が設定されていること
- 地域・地区APIフィルターが一致すること
- 地図に表示される座標が青森市および該当地域の範囲内であること
- HTMLエスケープと外部リンクの `noopener noreferrer` を確認すること

テストが環境問題で実行できない場合は、実行できなかった理由を明記し、代替のFlaskテストクライアントまたは構文チェックを実行する。最後に、変更したファイル、実装した機能、実行した確認結果を簡潔に報告する。
